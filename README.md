# FOR THE BLOCKCHAIN EMR TEAM

1. `npm install`
1. `npm start`

## VS Code Linting

Add the following in your `.vscode/settings.json` file

```json
  "editor.formatOnPaste": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
```
